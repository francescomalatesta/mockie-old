<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Mockie! - Random Data Generator</title>

        <link rel="stylesheet" href="<%= asset('assets/css/bootstrap.min.css') %>">
        <link rel="stylesheet" href="<%= asset('assets/css/bootstrap-theme.min.css') %>">

        <link href='https://fonts.googleapis.com/css?family=Roboto' rel='stylesheet' type='text/css' />

        <link rel="stylesheet" href="<%= asset('assets/css/style.css') %>">
    </head>
    <body id="application">
        <div id="wrapper">
            <div id="sidebar-wrapper">
                <ul class="sidebar-nav">
                    <li class="sidebar-brand">
                        <h1>Mockie!</h1>
                        <h2 class="subtitle">Random Data Generator</h2>
                    </li>
                    <li>
                        <a href="#" @click="showFieldsSetsSidebar = true">Fields Sets</a>
                    </li>
                </ul>
            </div>

            <div id="page-content-wrapper">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-lg-12">
                            <h1>Hey there.</h1>
                            <p>Time to generate some random data, baby!</p>

                            <hr />

                            <div>
                                <div class="row">
                                    <div class="col-md-7">
                                        <h2>Fields</h2>
                                        <p>1. Choose your fields and their types.</p>

                                        <div id="fields">
                                            <div class="row field" v-for="field in fields" v-on:keyup="refreshPreview">
                                                <div class="col-md-5">
                                                    <input type="text" class="form-control" placeholder="Field Name..." v-model="field.name" />
                                                </div>
                                                <div class="col-md-5">
                                                    <button class="btn btn-info form-control" v-on:click="setSelectedField($index)" @click="showFieldTypeSidebar = true">
                                                        <span v-if="field.type === null">Choose Type</span>
                                                        <span v-else>Chosen Type: {{ field.typeLabel }}</span>
                                                    </button>
                                                </div>
                                                <div class="col-md-2">
                                                    <button class="btn btn-danger form-control" v-on:click="removeField($index)">Remove</button>
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-3">
                                                    <button class="btn btn-default form-control" v-on:click="saveCurrentSet()">Save Fields Set</button>
                                                </div>

                                                <div class="col-md-9">
                                                    <button class="btn btn-success form-control" v-on:click="addField">+ Add Field</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-5">
                                        <h2>Preview</h2>
                                        <p>2. Here's an example of what you're going to generate.</p>
                                        <pre>{{ previewContent | json }}</pre>
                                    </div>
                                </div>

                                <hr />

                                <div class="row">
                                    <div class="col-md-6">
                                        <h3>Count</h3>
                                        <p>3. How many items you want to generate?</p>
                                        <p><input type="text" class="form-control" placeholder="Items Count..." v-model="items_count" /></p>
                                    </div>
                                    <div class="col-md-6">
                                        <h3>Output Format</h3>
                                        <p>4. Choose the output format for your generated data.</p>
                                        <p>
                                            <select class="form-control" v-model="output_format">
                                                <option v-for="format in available_output_formats" value="{{ format.extension }}">{{ format.name }}</option>
                                            </select>
                                        </p>
                                    </div>
                                </div>

                                <hr />

                                <div class="row">
                                    <div class="col-md-12">
                                        <button class="btn btn-success form-control generateButton" v-on:click="generateFile">
                                            <span v-if="isGenerating">Generating your file, please wait...</span>
                                            <span v-else>Generate {{ items_count }} items in one nice file!</span>
                                        </button>
                                    </div>
                                </div>

                                <sidebar :show.sync="showFieldTypeSidebar" placement="right" header="Choose Field Type" :width="550">
                                    <div class="row">
                                        <div class="col-md-12">
                                            <h3>Choose the type for the <b>"{{ fields[currentlySelectedField].name }}"</b> field.</h3>

                                            <hr>

                                            <p>
                                                <select class="form-control" v-model="chosenFieldTypeCategory">
                                                    <option value="{{ $index }}" v-for="fieldTypeCategory in availableFieldTypeCategories">
                                                        {{ fieldTypeCategory.name }}
                                                    </option>
                                                </select>
                                            </p>

                                            <hr />

                                            <h3>Category: {{ availableFieldTypeCategories[chosenFieldTypeCategory].name }}</h3>
                                            <p v-for="type in availableFieldTypeCategories[chosenFieldTypeCategory].types">
                                                <button class="btn btn-default form-control" v-on:click="changeFieldType(currentlySelectedField, $index)">{{ type.name }}</button>
                                            </p>
                                        </div>
                                    </div>
                                </sidebar>

                                <sidebar :show.sync="showFieldsSetsSidebar" placement="right" header="Choose Fields Set" :width="550">
                                    <div class="row">
                                        <div class="col-md-12">
                                            <h3>Choose a fields set from your saved collection.</h3>

                                            <hr />

                                            <div v-for="set in fieldsSets">
                                                <div class="row">
                                                    <div class="col-md-8">
                                                        <h4 class="set-name">{{ set.name }}</h4>
                                                        <p class="set-fields"><i>{{ set.fields | show_names }}</i></p>
                                                    </div>
                                                    <div class="col-md-4">
                                                        <p><button class="btn btn-success form-control" v-on:click="useSavedFieldsSet($index)">Use Set</button></p>
                                                    </div>
                                                </div>

                                                <hr class="set-separator" />
                                            </div>

                                            <p v-if="fieldsSets.length == 0"><i>Oops, no saved sets right now! To save one, just choose the fields you want, and then click on "Save Fields Set".</i></p>
                                        </div>
                                    </div>
                                </sidebar>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <script src="<%= asset('assets/vendor/vue/dist/vue.js') %>"></script>
        <script src="<%= asset('assets/vendor/vue-strap/dist/vue-strap.js') %>"></script>
        <script src="<%= asset('assets/js/bundle-min.js') %>"></script>
    </body>
</html>
